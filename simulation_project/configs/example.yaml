project:
  name: example-materials-workflow
  description: Placeholder config for Arbor-driven simulation experiments.

inputs:
  material_formula: Fe
  structure_source: demo

evaluation:
  target_metric: score
  higher_is_better: true
  output_metrics: outputs/metrics.json
